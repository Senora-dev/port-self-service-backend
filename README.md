# self-service-backend-dev
TODO:
- Generate JWT inside the CodeBuild because it's expired!!!
- Change secrets arns in the buildspec to be inserted by tf (agent outputs).
- Update READMEs that mention JWT.